.codebuddy/
.vscode/
java_path/
Java_path/
notepad/
python3/
tools/*
# 例外：以下目录是「仓库自带源码」——天狐原创脚本、以及随手写的一次性 CLI。
# 它们没有任何公开下载源，provision_tools.py 也装不回来；若被 tools/* 连带忽略，
# clone 下来的工具箱里这些工具会直接消失（界面有卡片、一点就报路径不存在）。
!tools/oracle-py/
!tools/revshell/
!tools/webshell/
!tools/brute/
# 注意：这里只能放行**源码**。provision 下来的二进制也落在同一个目录里，
# 整目录放行会把 30MB+ 的 dbx-mcp 一起提交进去（犯过一次）。
# 所以写成「放行目录 + 挡掉二进制」，靠目录内的 .gitignore 逐文件放行。
!tools/dbx/
!tools/sstikit/
!tools/springkit/
!tools/tomcatscanpro/
!tools/dedecmscan/
!tools/rediskit/
!tools/ruoyikit/
!tools/avoidkilling/
!tools/docem/
!tools/dirsearch/
*.pyc
*.jpg
*.png
*.ico
!config/fox.png
!config/fox.ico
# 文档用的海报与界面截图需要随仓库分发，上面的 *.png 会误伤，这里放行
!docs/**/*.png
*.bak
*.zip
__pycache__/
app.log
config/.instance.lock
config/shortcuts.ini
config/settings.json
# 工具使用计数：每启动一次 +1，是本机运行时状态，不入库
config/weights.json

# config/tools.json 故意不忽略：它是工具箱的核心工具清单（63 条），
# 内容全是相对路径（nmap、/tools/xxx），不含任何本机信息。
# 若把它排除，clone 下来的仓库就是个空壳——有代码有二进制，但界面里没有任何工具。

agent.md
AGENTS.md

# 二开新增
.buildtools/
output/
